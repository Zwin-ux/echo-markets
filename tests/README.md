# Tests

This directory is for all automated tests.

- `unit/` for unit tests
- `integration/` for integration tests
- `e2e/` for end-to-end tests

Use Jest and React Testing Library for unit/integration, and Cypress or Playwright for e2e.
