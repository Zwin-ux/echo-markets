{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-postgresql://echo:echo@localhost:5432/echo_markets}\" DIRECT_URL=\"${DIRECT_URL:-postgresql://echo:echo@localhost:5432/echo_markets}\" && pnpm exec prisma generate && pnpm run build"
  },
  "deploy": {
    "startCommand": "pnpm run start"
  }
}
